# Django
Django>=5.1,<6.0
gunicorn>=22.0
uvicorn[standard]>=0.30

# Database
psycopg[binary]>=3.1

# Cache / Queue
redis>=5.0
celery[redis]>=5.4
django-celery-beat>=2.6

# Auth (Clerk only)
# Pisos elevados por advisories (pip-audit): PyJWT <2.13.0 e cryptography <48.0.1
# têm vulnerabilidades — e PyJWT valida o JWT do Clerk em core/middleware.py.
PyJWT>=2.13.0
cryptography>=48.0.1

# Storage
django-storages[google]>=1.14
google-cloud-storage>=2.16
whitenoise>=6.7

# LLM Integrations
# SDK novo do Gemini: o código usa `from google import genai` + genai.Client +
# client.models.generate_content (API google-genai 2.x). O pacote antigo
# google-generativeai NÃO fornece esses símbolos — usá-lo quebra em produção
# com ImportError: cannot import name 'genai' from 'google'.
google-genai>=2.0,<3
anthropic>=0.34

# RAG — busca vetorial local (substitui Vertex AI / Discovery Engine)
pgvector>=0.3
sentence-transformers>=2.2
# torch CPU-only: o Railway roda em CPU, então as libs CUDA do wheel padrão
# são peso morto (~+1-2 GB de imagem). O índice cpu do PyTorch entrega a build
# enxuta. Pin casado com o wheel cp314/manylinux_2_28 disponível.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.1+cpu

# Monitoring
sentry-sdk[django,celery,redis]>=2.8

# Payments
stripe>=10.0

# PDF Export / Processing
weasyprint>=62.0
PyMuPDF>=1.24

# OCR (Document AI)
google-cloud-documentai>=2.0

# Utils
python-dotenv>=1.0
requests>=2.32
markdown>=3.6

# Dev
django-silk>=5.1
